# Farm recount — TREASON only (2026-08-09)

**Filter (same as farm docs):**  
`treason === true` ∧ ¬`slotDegraded*` ∧ ¬(`quit` ∧ `ticks < 100`) ∧ ¬legacy `ZRG8-m1`

**Source:** unique `(sid, matchIndex)` across `logs/**` + `reports/**` session match JSON.

## Headline

| | Published (outcomes table / essay addendum) | **Recount** |
|---|---:|---:|
| Corpus **n** | **128** | **127** |
| Why Δ | `97+8+13+4+6` fold | The **+8** Qwen fold counted **PQRS-m1** (`quit`, `ticks=0`) — must drop under the same filter |

`8VFN` m6–m12 are **TREASON off** — correctly **not** in either table.  
Live `5BMY` / `WMDG` (ice / speech runs) are **TREASON off** — not in betrayal farm.

Base without RHVQ/KAW8/8VFN/PQRS/XSGD: **97** (matches published base).

## TREASON-off in dumps (excluded)

| Session | Matches | Note |
|---|---|---|
| 8VFN m6–m12 | 7 | Explicitly not folded |
| 5BMY, WMDG | (docker live) | Ice / speech; no match JSON in farm dumps yet |

## Kimi / Qwen (essay claims vs TREASON-only)

Unique matches where the model appears in either slot, after the filter above.

| | Essay (`kimi-qwen-…` / addendum prose) | **TREASON-only recount** |
|---|---|---|
| **Kimi** games | 13 | **10** (drops RHVQ-m2/m4/m13: `quit`∧`ticks=0`) |
| Kimi arm (`veilcut` true in plans) | 1 / 13 | **1 / 10** (still only KAW8-m2) |
| Kimi fire (`fireTick`) | 0 / 13 | **0 / 10** |
| **Qwen** games | ~16 | **20** (PQRS keepable + RHVQ + KAW8-m3 + 8VFN m0–m5) |
| Qwen arm | ~12 / 16 | **13 / 20** |
| Qwen fire | ~11 / 16 | **11 / 20** |
| Qwen initiates (first `fireTick`) | ~5 | **8 / 20** |

Arm/fire still support the same qualitative split (Qwen winters, Kimi almost never). Denominators were inflated by quit@0 (Kimi) / undercounted later 8VFN (Qwen).

## Outcomes table (slot0\|slot1 games)

Published TOTAL **128\|128**. Recount TOTAL **127\|127**. Per-model games shift by at most −1 where PQRS-m1 contributed (Qwen slot0). Win/loss columns need a careful traitor attribution pass before replacing the PNG — games/betrayal counts are trustworthy after the n fix; initiator inference for neglect rows is noisier in a blind recount.

**Action:** published **n=128 → n=127**; chart regenerated 2026-08-09 (`…-2026-08-09.png` + `docs/assets/`); n=128 PNG archived as `…-n128.png`. Do not fold TREASON-off ice sessions into betrayal stats.

## Wraith resolutions

`reports/wraith-resolutions.*` currently mixes `treason=on` and `off` (e.g. 8VFN-m7/m9/m12). For betrayal-adjacent claims use **treason=on only**; ice-gate / kill-gate notes stay separate.
